const form=document.getElementById('demoPaymentForm');const card=document.getElementById('cardNumber');const expiry=document.getElementById('expiry');card?.addEventListener('input',()=>{card.value=card.value.replace(/\D/g,'').slice(0,16).replace(/(.{4})/g,'$1 ').trim()});expiry?.addEventListener('input',()=>{let v=expiry.value.replace(/\D/g,'').slice(0,4);expiry.value=v.length>2?v.slice(0,2)+'/'+v.slice(2):v});form?.addEventListener('submit',e=>{e.preventDefault();document.getElementById('paymentDemoMsg').textContent='Demo approved. In the live version, Stripe would confirm the CAD 50 payment and return the guest to the reservation confirmation page.';form.querySelector('button').disabled=true;});